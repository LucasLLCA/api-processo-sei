"""Shared helpers for SEAD pipeline scripts (ETL, NER, enrichment, inspection).

This package is self-contained and must not import from the top-level `api/`
package, so ETL scripts can run in minimal environments.
"""
